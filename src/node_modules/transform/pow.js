'use strict';

const through = require('through2');

module.exports = function(p) {
    return through(function(c, e, n) {
        this.push('' + Math.pow(parseInt(c.toString()), p));
        n();
    });
};
