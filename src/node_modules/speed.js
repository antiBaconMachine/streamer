'use strict';

exports.slider = function(min, max, cb) {
    const slider = document.createElement('input');
    slider.type = 'range';
    slider.min = min;
    slider.max = max;
    slider.value = '500';
    slider.step = '25'
    slider.onchange = cb;
    return slider;
}
