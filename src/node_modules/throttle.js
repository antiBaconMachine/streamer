'use strict';

exports.throttle = function(delay, stream){
    const write = stream._write.bind(stream);
    if (write) {
        stream._write = function(c, enc, cb) {
            setTimeout(() => write(c, enc, cb), delay.interval());
        };
    }
    return stream;
};
